# How to create a java spring boot project without web support

```bash
spring init --dependencies=lombok --language=java --build=maven --type=maven-project --package-name=cl.spring.tcp.example spring-tcp
```